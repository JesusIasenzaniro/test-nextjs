import React from 'react';

const Loading = () => {
    return (
        <section className='text-2xl font-bold text-sky-500'>
            Loading...
        </section>
    );
};

export default Loading;
